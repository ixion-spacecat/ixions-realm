---
title: Ixion's Blog
layout: layouts/blog-list.njk
redirect_from: /posts
---

<h1>Ixion's Blog</h1>
<p>Where I talk about web development and whatever I want ^.^</p>
<ul class="blog--list">
  {%- for post in collections.post | reverse -%}
    <li class="blog--item">
      <div class="blog--item--container">
        <div class="blog--item--title">
          <a href="{{ post.page.url }}">{{ post.data.title }}</a>
        </div>
        <div class="blog--item--date">
          {%- if post.data.draft %}
            DRAFT
          {%- else %}
            {{ post.page.date | readableDate }}
          {%- endif %}
        </div>
        <div class="blog--item--subtitle">{{ post.data.subtitle }}</div>
        <div class="blog--item--readtime">{{ post.content | readingTime }} read</div>
      </div>
    </li>
  {%- endfor -%}
</ul>